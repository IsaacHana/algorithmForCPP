#include <bits/stdc++.h>
using namespace std;

int dp[1001];

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    // 1 | -> 1
    // 2 || (1 + 1.1) , =, ¤± ->  3
    // 3 ||| (1 + 2.1), |= (1 + 2.2), |¤± (1 + 2.3) -> 3 Àü¿¡²¨¿¡ ÁÂÃø¿¡ | Ãß°¡
    //   =| (2.2 + 1), ¤±| (2.3 + 1) -> 2 ÀüÀü²¨¿¡ ÁÂÃø¿¡ =,¤± Ãß°¡
    //   -> 5 (3 + 1 * 2)
    //
    // 4 |||| (1 + 3.1), ||= (1 + 3.2), ||¤± (1 + 3.3), |=| (1 + 3.4), |¤±| (1 + 3.5) -> 5
    //   =|| (3.4 + 1), ¤±||(3.5 + 1), == (2.2 + 2.2), ¤±¤±(2.3 + 2.3), =¤±, ¤±= -> 6
    //   -> 11 (5 + 3 * 2)

    int n;
    cin >> n;

    dp[1] = 1;
    dp[2] = 3;
    for (int i = 3; i <= n; i++) {
        dp[i] = (dp[i - 1] + dp[i - 2] * 2) % 10'007;
    }

    cout << dp[n] << '\n';
}